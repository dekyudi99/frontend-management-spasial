name: Deploy Biomass Mangrove

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy langsung di folder project
        run: |
          cd /root/biomass-manggrove
          git pull origin main
          docker compose up -d --build
          docker image prune -f